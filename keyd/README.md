Download keyd from its repository, then make a debian package out of it.

## Required packages to build
- the usual to build debian packages from source including `fakeroot`
- and `make`, `git`, `patch`, `gcc`, `scdoc`
